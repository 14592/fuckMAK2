<mvc:View
        xmlns:mvc="sap.ui.core.mvc"
        xmlns="sap.m"
        xmlns:f="sap.ui.layout.form"
        controllerName='de.nak.minibar.controller.Detail'>
    <Page
            title="{i18n>main.PageTitle}"
            showNavButton="true"
            navButtonPress="onNavButtonPress">
        <headerContent>
            <Button text="{i18n>main.SCButton}" press="onSCButtonPress"/>
        </headerContent>
        <content>
            <Toolbar>
                <Title text="{minibar>Name}" level="H1"/>
            </Toolbar>



<!--            <f:SimpleForm
                    layout="ResponsiveGridLayout">
                <f:content>
                    <Label text="{minibar>Price}" level="H3"/>
                    <TextArea id="inputDate" value="{minibar>Description}" enabled="false" height="auto"/>
                </f:content>
            </f:SimpleForm>-->
        </content>
    </Page>
</mvc:View>
