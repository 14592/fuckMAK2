<mvc:View
        xmlns:mvc="sap.ui.core.mvc"
        xmlns="sap.m"
        controllerName='de.nak.minibar.controller.Shoppingcart'>
    <Page title="{i18n>main.PageTitle}"
          showNavButton="true"
          navButtonPress="onNavButtonPress">
        <content>
            <List headerText="{i18n>main.headerText}"
                  items="{ path: 'minibar>/SHOPPINGCARTSet'}"
                  growing="true"
                  growingThreshold="100"
                  enableBusyIndicator="true">
                <headerToolbar>
                    <Toolbar>
                        <Title text="{i18n>mainPageText}" level="H2"/>
                    </Toolbar>
                </headerToolbar>
                <StandardListItem title="{minibar>Matnr}"
                                  textarea="{minibar>Amount}"
                />
            </List>
        </content>
    </Page>
</mvc:View>
